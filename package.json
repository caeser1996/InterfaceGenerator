{
  "name": "generate-interface",
  "version": "1.0.0",
  "description": "A helper to generate interface and type definitions in TypeScript",
  "main": "index.js",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1"
  },
  "keywords": [
    "typescript",
    "interface",
    "type",
    "definition"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "commander": "^4.1.0"
  }
}
